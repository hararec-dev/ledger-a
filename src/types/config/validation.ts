export interface ValidationConstants {
    ONBOARDING_SETUP: {
        REQUIRED: {
            currency: string;
            accountName: string;
            initialAmount: string;
        };
        NUMERIC: {
            positiveNumber: string;
        };
    };
}